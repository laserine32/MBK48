import PackForm from "@/components/shared/form/pack-form"

export const metadata = {
	title: "Add Pack",
}

const AddPackPage = () => {
	return (
		<>
			<PackForm pageTitle={"Add Pack"} />
		</>
	)
}

export default AddPackPage
